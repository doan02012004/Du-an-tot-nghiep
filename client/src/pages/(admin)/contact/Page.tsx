import React from 'react'
import { Outlet } from 'react-router-dom'

type Props = {}

const PageContactAdmin = (props: Props) => {
  return (
    <>
        <Outlet />
    </>
  )
}

export default PageContactAdmin
